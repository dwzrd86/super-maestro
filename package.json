{
  "name": "agentforge",
  "version": "0.2.0",
  "description": "Unified AI Agent Orchestrator - Desktop-native with persistent intelligence and distributed execution",
  "private": true,
  "dependencies": {
    "7zip-bin": "~5.2.0",
    "app-builder-bin": "~4.0.0",
    "async-exit-hook": "^2.0.1",
    "patch-package": "^8.0.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "lib/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules",
    "postinstall": "patch-package",
    "dev:web": "turbo run dev --filter=@agentforge/web",
    "dev:desktop": "npm run dev --workspace @agentforge/desktop",
    "build:web": "turbo run build --filter=@agentforge/web",
    "dev:runner": "turbo run dev --filter=@agentforge/runner",
    "mesh:start": "turbo run dev --filter=@agentforge/mesh",
    "cozo:test": "turbo run test --filter=@agentforge/cozo",
    "test": "vitest run",
    "prep:desktop": "npm run build:web && npm run build --workspace @agentforge/runner",
    "package:appimage": "npm run prep:desktop && npm run package:appimage --workspace @agentforge/desktop",
    "package:mac": "npm run prep:desktop && npm run package:mac --workspace @agentforge/desktop",
    "package:win": "npm run prep:desktop && npm run package:win --workspace @agentforge/desktop",
    "package:linux": "npm run prep:desktop && npm run package:linux --workspace @agentforge/desktop",
    "package:deb": "npm run prep:desktop && npm run package:deb --workspace @agentforge/desktop",
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy",
    "convex:codegen": "npx convex codegen",
    "convex:dashboard": "npx convex dashboard"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "overrides": {
    "tar": "^7.5.9",
    "@mapbox/node-pre-gyp": {
      "tar": "^7.5.9"
    },
    "minimatch": "^10.2.1",
    "glob": "^11.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.2.0",
  "license": "AGPL-3.0"
}
